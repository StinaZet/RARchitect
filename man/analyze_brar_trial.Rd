% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_brar_trial.R
\name{analyze_brar_trial}
\alias{analyze_brar_trial}
\title{Analyze a Single BRAR Trial}
\usage{
analyze_brar_trial(
  outcome_type = c("binary", "cont"),
  distribution = c("bernoulli", "normal", "exponential"),
  trial_data,
  N,
  arms,
  direction = c("lower", "higher"),
  priors,
  known_var = NULL,
  estimation_method = c("MLE", "IPW", "post_mean"),
  test_method = c("wald", "exact", "randomization", "AP", "simulation"),
  CI_method = c("wald", "simulation"),
  effect_measure = "riskdifference",
  multiple_tests = FALSE,
  onesided = TRUE,
  B = 10000,
  alpha = 0.05,
  prob_threshold = NULL,
  ...
)
}
\arguments{
\item{outcome_type}{Character. Specifies the type of outcome to simulate.
Must be either \code{"binary"} or \code{"cont"}.}

\item{distribution}{Character. Specifies the distribution of the outcome.
Must be either \code{"bernoulli"}, \code{"normal"}, or \code{"exponential"}.}

\item{trial_data}{Data frame containing trial results.
Must include columns 'Arm' and 'Outcome'.}

\item{N}{Total sample size.}

\item{arms}{Number of treatment arms.}

\item{direction}{\code{"lower"} or \code{"higher"} (indicates which outcome is favorable).}

\item{priors}{Matrix of prior parameters for Bayesian estimation (2 x K for binary).}

\item{known_var}{Logical, for normal outcomes with known variance.}

\item{estimation_method}{\code{"MLE"}, \code{"IPW"}, or \code{"post_mean"}.}

\item{test_method}{\code{"wald"}, \code{"exact"}, \code{"randomization"}, \code{"AP"}, or \code{"simulation"}.}

\item{CI_method}{\code{"wald"} or \code{"simulation"}.}

\item{effect_measure}{Currently only \code{"riskdifference"} supported.}

\item{multiple_tests}{Logical; if \code{TRUE}, test all experimental arms vs control with Bonferroni adjustment.}

\item{onesided}{Logical; if \code{TRUE}, perform one-sided test.}

\item{B}{Number of simulations/permutations for simulation/randomization-based tests.}

\item{alpha}{Significance level (used for CI and tests).}

\item{prob_threshold}{Posterior probability threshold (for Bayesian decision rules).}

\item{...}{Additional arguments passed to test methods (e.g. \code{randmethod}, \code{blocksize}, \code{postprobmethod}, \code{multiarm_method}).}
}
\value{
A list with:
\describe{
\item{test_results_df}{Data frame with estimated effects, confidence intervals, and p-values.}
\item{patient_benefit}{Number of patients allocated to the best estimated arm.}
\item{mean_outcome}{Overall mean outcome across patients.}
}
}
\description{
Wrapper function to analyze a single BRAR or Fixed Randomization (FR) trial replicate
with binary, normal, or exponential outcomes.

Returns estimated treatment effects, p-values, and confidence intervals.
Also reports patient benefit (number of patients on the estimated best arm).
}
\details{
\strong{Notes:}
\itemize{
\item MLE estimation may be biased under adaptive randomization.
\item Wald test and Wald confidence intervals may not be valid for BRAR data.
\item For adaptive designs, prefer "post_mean" or "IPW" estimation and "randomization" or "AP" testing.
}
}
\examples{
# --- Example setup: simulate a BRAR trial ---
set.seed(103)
results_binary_multiarm = simulate_brar_trial(
  outcome_type = "binary",
  distribution = "bernoulli",
  direction = "higher",
  arms = 3, N = 150, burnin = 15, blocksize = 1,
  priors = matrix(c(1, 1, 1, 1, 1, 1), nrow = 2, byrow = TRUE),
  modelpar = c(0.5, 0.7, 0.8),
  tuning = 1,
  clipping = 0,
  randmethod = "coin",
  postprobmethod = "simulation",
  recruitment_rate = 8,
  observation_delay = 20)

# --- Example 1: Simulation-based test ---
analyze_brar_trial(outcome_type = "binary",
  trial_data = results_binary_multiarm,
  N = 150, arms = 3, direction = "higher",
  priors = matrix(c(1, 1, 1, 1, 1, 1), nrow = 2, byrow = TRUE),
  distribution = "bernoulli", burnin = 15,
  postprobmethod = "simulation",
  blocksize = 1, estimation_method = "IPW",
  test_method = "simulation", CI_method = "simulation",
  B = 10)

# --- Example 2: Wald test (not recommended for BRAR) ---
analyze_brar_trial(outcome_type = "binary",
  trial_data = results_binary_multiarm,
  N = 150, arms = 3, direction = "higher",
  priors = matrix(c(1, 1, 1, 1, 1, 1), nrow = 2, byrow = TRUE),
  distribution = "bernoulli", postprobmethod = "simulation",
  estimation_method = "MLE",
  test_method = "wald", CI_method = "wald")

# --- Example 3: AP test (Allocation-Probability test) ---
analyze_brar_trial(outcome_type = "binary",
  trial_data = results_binary_multiarm,
  N = 150, arms = 3, direction = "higher",
  priors = matrix(c(1, 1, 1, 1, 1, 1), nrow = 2, byrow = TRUE),
  distribution = "bernoulli", postprobmethod = "simulation",
  estimation_method = "IPW",
  test_method = "AP", CI_method = "simulation",
  randmethod = "coin", blocksize = 1, B = 10,
  postprobmethod = "simulation", multiarm_method = "top2")

}
