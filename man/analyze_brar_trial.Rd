% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_brar_trial.R
\name{analyze_brar_trial}
\alias{analyze_brar_trial}
\title{Analyze a Single BRAR/FR Trial Replicate}
\usage{
analyze_brar_trial(
  trial_data,
  N,
  arms,
  direction,
  priors,
  distribution,
  known_var = NULL,
  estimation_method = c("MLE", "IPW", "post_mean"),
  test_method = c("wald", "exact", "randomization", "AP", "simulation"),
  CI_method = c("wald", "simulation"),
  effect_measure = "riskdifference",
  multiple_tests = FALSE,
  onesided = TRUE,
  alpha = 0.05,
  prob_threshold = NULL,
  ...
)
}
\arguments{
\item{trial_data}{Data frame with trial results.
Must include columns 'Arm' and 'Outcome'.}

\item{N}{Total sample size.}

\item{arms}{Number of treatment arms.}

\item{direction}{"lower" or "higher" (indicates which outcome is favorable).}

\item{priors}{Matrix of prior parameters for Bayesian estimation (2 x K for binary).}

\item{distribution}{"bernoulli", "normal", or "exponential".}

\item{known_var}{Logical, used only for normal outcomes with known variance.}

\item{estimation_method}{"MLE", "IPW", or "post_mean" (for binary outcomes).}

\item{test_method}{"wald", "exact", "randomization", "AP", or "simulation".}

\item{CI_method}{"wald" or "simulation".}

\item{effect_measure}{Currently only "riskdifference" supported for binary outcomes.}

\item{multiple_tests}{Logical; if TRUE, test all experimental arms vs control with Bonferroni adjustment.}

\item{onesided}{Logical; if TRUE, perform one-sided test.}

\item{alpha}{Significance level (used for CI and tests).}

\item{prob_threshold}{Posterior probability threshold (for future Bayesian use).}

\item{...}{Additional arguments passed to test methods (e.g., B, policy_file, randmethod, blocksize, postprobmethod, multiarm_method).}
}
\value{
List with components:
\itemize{
\item test_results_df: data frame with effect estimates, p-values, and CIs.
\item patient_benefit: number of patients on estimated best experimental arm.
\item mean_outcome: mean outcome across all patients.
}
}
\description{
Wrapper function to analyze a single BRAR or Fixed Randomization trial replicate
with binary, normal, or exponential outcomes.
Returns effect estimates, p-values, and confidence intervals (no coverage).
Also reports patient benefit = number of patients on estimated best arm.
}
\details{
Warnings are issued if methods may not be appropriate for BRAR data:
\itemize{
\item MLE estimation may be biased under adaptive randomization.
\item Wald test and Wald confidence intervals may not be valid under BRAR.
}
}
\examples{
# Simulation-based test
analyze_brar_trial(
  trial_data = trial_df, N = 100, arms = 3, direction = "higher",
  priors = matrix(c(1,1,1,1,1,1), nrow = 2),
  distribution = "bernoulli",
  test_method = "simulation", CI_method = "simulation",
  B = 5000
)

# Wald test (with warnings)
analyze_brar_trial(
  trial_data = trial_df, N = 100, arms = 3, direction = "higher",
  priors = matrix(c(1,1,1,1,1,1), nrow = 2),
  distribution = "bernoulli",
  test_method = "wald", CI_method = "wald"
)

# AP test example
analyze_brar_trial(
  trial_data = trial_df, N = 100, arms = 3, direction = "higher",
  priors = matrix(c(1,1,1,1,1,1), nrow = 2),
  distribution = "bernoulli",
  test_method = "AP", CI_method = "simulation",
  randmethod = "block", blocksize = 5, postprobmethod = "simulation"
)

}
