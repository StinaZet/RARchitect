% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brar_randomization.R
\name{brar_randomization}
\alias{brar_randomization}
\title{Generate Randomization Sequences for a BRAR Trial}
\usage{
brar_randomization(
  trial_data,
  priors,
  blocksize,
  direction = c("higher", "lower"),
  postprobmethod = c("simulation", "exact"),
  multiarm_method = c("top2", "fixed"),
  tuning = 1,
  clipping = 0,
  urn_alpha = NULL,
  randmethod = c("coin", "block", "urn"),
  return = c("allocations", "probabilities")
)
}
\arguments{
\item{trial_data}{Data frame with columns 'Arm', 'Outcome', and 'Batch'.}

\item{priors}{2 x K matrix of alpha/beta parameters for each arm.}

\item{blocksize}{Numeric. Block size for the next block in the BRAR trial.}

\item{direction}{Character. "higher" or "lower".}

\item{postprobmethod}{Character. "simulation" or "exact".}

\item{multiarm_method}{Character. "top2" or "fixed".}

\item{tuning}{Numeric. Tuning exponent for allocation probabilities.}

\item{clipping}{Numeric or "adaptive". Minimum allocation probability per arm.}

\item{urn_alpha}{Numeric. Urn alpha parameter (if randmethod = "urn").}

\item{randmethod}{Character. "coin", "block", or "urn".}

\item{return}{Character. Either "allocations" (default) or "probabilities".}
}
\value{
Either a vector of randomized allocations or a matrix of allocation probabilities.
}
\description{
Generates randomization sequences or allocation probabilities for a BRAR trial
given observed outcomes, priors, and allocation settings.
}
